import { Text, TouchableOpacity } from 'react-native';
import React from 'react';

const LoginButton = () => {
  return (
    <TouchableOpacity>
      <Text>Login</Text>
    </TouchableOpacity>
  );
};

export default LoginButton;
